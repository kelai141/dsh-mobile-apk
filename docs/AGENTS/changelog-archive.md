# changelog-archive.md — 更新记录表历史归档

> 全部版本历史都堆在这里（AGENTS.md 不再设更新记录表）。**新条目倒序加在表格最上方**。细节写进对应详档，这里只留一行摘要。

| 时间 | 版本 | 更新内容 | 更新者 |
|---|---|---|---|
| 2026-09-19 | **0.14.1 预览轮 / P0 真机 API 越级崩溃修复（未提交/未验收，分支 feat/0141-preview）** | **真机反馈（华为 NOH-AN00 / Android 31 / 出厂 0.14.0 vc39）**：`engine-died-during-boot` exit=1，logcat 12:55/12:56/12:57 三时点反复 `NoSuchMethodError: No interface method toList()Ljava/util/List; in class Ljava/util/stream/Stream;` → `SnapshotFs.deletePath(SnapshotFs.kt:50)` → `SnapshotTransaction.finish` → `EngineManager.applyRecovery`。**真因**：`Files.list(dir).use { it.toList() }` 的 `toList()` 是 **Java `Stream.toList()`（API 34）**而非 Kotlin stdlib 扩展；minSdk 26 → API<34 设备无此接口方法。**后果链**：`NoSuchMethodError` 是 **Error 不是 Exception**，`catch (e: Exception)` 抓不住 → 打穿恢复链 → **快照刷新/恢复在 Android<34 永久卡死（marker retained）**。**修法**：改用 `Files.newDirectoryStream(dir).use { it.toList() }`（`DirectoryStream` 是 `Iterable`+`Closeable`，Kotlin `toList()` 是 stdlib 扩展，无 API 级别依赖；`use` 保证关闭；语义等价：只列直接子项、不跟随符号链接、项读取失败记 onFailure 并跳过）。**全仓 API 审计（115 个 .kt）**：命中 1 处（即本坑），已修；`SnapshotUserData.kt:106` 的 `Stream.forEach`（since=24）与 `SnapshotTransaction.kt:240` 的 `File.listFiles()` 均判安全；全仓 23 处 `.toList()` 其余 22 处接收者为 Kotlin 集合/Sequence（安全）。**新增防线** `ApiLevelGuardTest`：禁用清单（`Stream.toList`/`ofNullable`/`dropWhile`/`takeWhile`、`Collectors.toUnmodifiable*`、`Files.readString/writeString/mismatch`、`String.strip*/stripIndent/formatted`、`List|Map|Set.of`、`requireNonNullElse`）+ **词法扫描器**（先剥字符串再剥注释，兼容跨行链式），带 13 项反证与零假红自检；**并修掉扫描器自身的自伤缺陷**（朴素「本行含块注释起始符」实现被 MIME 字面量误入块注释态、静默漏扫 3273 行/18%）。**注**：`isBlank()` 经 javap 对 `app/build/tmp/kotlin-classes` 实证解析为 `kotlin/text/StringsKt.isBlank`（stdlib，安全），**不**列入禁用以免大面积假红。**文档**：坑 153、modules.md、ARCHITECTURE.md。 | overlayui |
| 2026-09-19 | **0.14.1 预览轮 / T5 块H+块I（未提交/未验收，分支 feat/0141-preview）** | **块I 悬浮球**：取消贴边强制吸附（删 `springSnapToEdge`/`cancelSpring`/`springAnim` 与 `androidx.dynamicanimation` 三个 import；壳侧该依赖从此零使用方，声明按详档判定保留并登记用途变化），保留最小边距并抽成唯一具名常量 `BALL_EDGE_MARGIN_DP=8`/`edgeMarginPx`（同时推导 `haloSizeDp`，整数恒等式被测试锁死），就地更正三处过期注释。新增状态描边 ring：`OverlayHalo.newHaloDrawable` 返回 **glow+ring 两层 `LayerDrawable`**（ring 硬边描边 2dp、内缩 4.5dp，落在球半宽与窗口半宽之间的可用环带内），`setHaloColors` 单函数同时写两层、颜色**同源**取自 `Halo.color`，`setHalo` 仍是唯一入口且脉冲逻辑一字未改（同 View → 天然同相位）；`as? GradientDrawable ?: return@post` 式静默失败改为显式取层 + `LogCollector` 诊断；`Halo` 枚举改**纯 Kotlin ARGB 字面量**（原 `Color.argb` 在 `isReturnDefaultValues=true` 下恒 0 → 颜色类单测假绿）。**块H 悬浮窗**：完成态常驻文案「已完成，长按查看汇报」——权威信号 `api-session/status running=false` 置位、语义标签取 `.live.ndjson turn_end.kind`（失败/被阻塞/被中断不得显示「已完成」），完成位放**服务级** `CompletionNotice`（面板收起态存活），`showPanel()` 首次打开消费一次、收起即回常态、新一轮与 tool_call 清除；报告栏长按打开（新增 `OverlayReport.kt`：独立顶层窗口 + ScrollView，内容取 T6 窄接口 `NotifyStore.latestReportLine()` 并经既有 `NotifyStore.parseEntry` 解析，复用 `reportLine/reportBigText` 文案口径；空摘要/无数据给占位行仍可打开；hidePanel/onDestroy 两处收口）；三击跳转（单一 OnTouchListener 状态机，长按优先、超 `scaledTouchSlop` 判拖动、`NEW_TASK\|SINGLE_TOP` 回 MainActivity 不重建 WebView）；长按期间置 `panelOccupied` 守卫自动收起。**测试**：新增 `OverlayHaloInvariantTest`（四态互不相同/字面量逐字节等价/多 density 恒等式与 ring 环带）+ `OverlayCompletionNoticeTest`（A1 无 pending 普通完成必显示、复位反证、语义不伪装、A2 空摘要与无数据不崩、A3 二次点击与长按互斥），`CallSiteContractTest` 追加 15 条断红线。**门禁**：`check-kotlin-comments.mjs` PASSED。**文档**：坑 147-150、modules.md、ARCHITECTURE.md、BRIDGE-API.md、DEPENDENCIES.md。 | overlayui |
| 2026-09-19 | **0.14.1 预览轮 / T6 块J+块L+块K壳侧 + 探针接线（未提交/未验收，分支 feat/0141-preview）** | **块J 通知实时化**：FIX-3 用户拍板取 A —— `DEFAULT_SUPPRESS_FOREGROUND = false`（前台也真发系统通知）；存量迁移**不动 prefs**（旧抑制系默认值造出、`setSuppressForeground` 全仓零调用，改默认值即修好存量；显式值原样保留并备份留痕、schema 代次只跑一次）。FIX-1 抑制=延后：新增 `NotifySuppressQueue.kt`（TTL 5min/容量 8/单次补投 3/同会话覆盖式去重/自续 tick），补投复用 `deliverDeferred`。FIX-2 listener 真实实现（`ShellListener` + `installShellListener`，复用 `flashStatus`、只依赖既有公开面）。FIX-4 设置读写能力（`settingsSnapshot`/`applySetting`，写后读回、未知 key 拒绝）。**块L**：`writeBootDiag` 落 `files/boot-diag.log`；页面侧 fiber 状态不可得时显式 `pageSideRuntime=unavailable`（**绝不留空数组**）。**块K**：NSC base-config 撑开明文 + 回环保留；`check-manifest-hardening.mjs` NSC 段同批改新形态（显式声明/回环逐字比对/拒无 domain 明文口子，**实测回退即判红**）+ JVM `BrowserHostCleartextConsistencyTest` 守跨层同向；**#183 影响书面记录**。**探针接线**：真因 = 产出面缺席（`[perf] TOTAL` 仅测量 preload 产物，且不在快照/overlay/注入链、argv/env 均不含）+ 解析链挂在**默认关闭**的调试采集器上；修法与 C1「首个 HTTP 响应」口径见坑 144。**文档**：坑 144-146、modules.md、ANDROID-API-USAGE.md。**交付**：T5 窄接口 `NotifyStore.latestReportLine(): String?`。 | shellcore |
| 2026-09-18 | **0.14.0 正式版（vc39，覆盖 v0.14.0-preview vc38）** | **本版主题：虚拟屏与 AI 浏览器达到可用线。** **虚拟屏**：跨屏拉起四条候选路径实测只有「壳侧 Shizuku UserService + `am start --display <id> -n <component>`」成立（monkey 无屏幕维度 / shell `am start` 不稳 / 进程内 `setLaunchDisplayId` 被 SafeActivityOptions 拒），据此把唯一可行路径做通；补 `screenId` 可发现性（此前该参数无 description，设备实录证明模型直接忽略 → App 落到真实屏）；截图两条通道都认屏幕（ADB 回落此前完全忽略 screenId，抓的是真实屏——静默错误答案）；等比 letterbox 按内容宽高比居中；可见性判据补「侧栏已收起」；序号复用（旧实现单调递增从不回收）；会话隔离 + 销毁不设锁 + 10 分钟空闲回收；手动关机按钮。**AI 浏览器**：隔离宿主 WebView 可用；分辨率=CSS 视口（document-start 注入 + letterbox，innerWidth 精确）；PC/手机身份；多页签；browser_snapshot 渲染每行 ref；侧栏收起时视口不再塌成 0x0（GONE → INVISIBLE + layoutDetached，实测 0x0→450x800）；setInitialScale 在 API 36 stub 已移除 → 改注入实现。**附件入口分流（#215）**：回形针单击即出菜单（Tooltip 气泡插在按钮与 input 之间致兄弟相邻判据失效，改为结构判定）；显式图片类型分流到系统相册（此前壳侧为修「空 accept 受限视图」把相册分支整个删掉，两个入口打开同一界面）。**Shizuku 时序**：ensureBound 只 await 一片 4s 而 bind 实测约 5s、且 bindLatch 一次性不清空 → 「第一次必失败第二次必成功」是确定结果；改为总预算内循环等待 + 三路径清 latch + connecting/not-bound 拆成两个 code。**快照升级健壮性**：deletePath 逐项容错 + 残留 stage 改名挪开（此前一个删不掉的损坏 stage 会让之后每次启动都失败）。**坑位 139-143 登记**（Termux 报错风暴真因 / 回形针兄弟判据 / Shizuku latch 时序 / 截图忽略 screenId / 图片 accept 被壳侧统一入口吞掉）。**验收**：门禁全绿、Kotlin 269 项 0 失败、插件 82+62+12、UI 180、双 ABI exit 0。 | kelai141 |