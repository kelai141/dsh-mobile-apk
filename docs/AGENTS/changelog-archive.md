# changelog-archive.md — 更新记录表历史归档

> 全部版本历史都堆在这里（AGENTS.md 不再设更新记录表）。**新条目倒序加在表格最上方**。细节写进对应详档，这里只留一行摘要。

| 时间 | 版本 | 更新内容 | 更新者 |
|---|---|---|---|
| 2026-09-23 | **修复：快照内 git 的 `credential.helper` / `!` alias / hook 全废（坑 172 / apk issue #247）** | git 执行凭据助手恒经 `run-command` 的 `use_shell=1`，而 shell 取**编译期** `SHELL_PATH`——Termux 预编译包把它写死为 `/data/data/com.termux/files/usr/bin/sh`（38 B），本壳前缀是 `…/com.dsharnessmobile.shell/files/usr` ⇒ 旧路径不存在，`credential.helper` 全族、`!` 前缀 alias、hook / `rebase --exec` 一律 `cannot exec`（真机实测；HTTPS 只能用 `GIT_ASKPASS` 绕）。关键是它**接不上 #87 那次修复**：`usr/bin/git` wrapper 覆盖的是 `--exec-path`（有 `GIT_EXEC_PATH`），而 `SHELL_PATH` **没有任何运行时覆盖点**，`relocate-snapshot.py`「ELF 不改写、功能路径由 `shellEnv()` 环境变量覆盖」的口径在这一项上没有落点；`check-prefix-residue.sh` 的 P4 只判「git 存在/`--version` 可跑」，旧串在场时**照样绿**，故长期未被发现。修法：git 来自 Termux 预编译包（配方改不了）⇒ 在 `relocate-snapshot.py` 对 git 家族白名单（`usr/bin/git.real` + `usr/libexec/git-core/*`，共 8 个 ELF）做**等长**替换 38 B → `/system/bin/sh`(14 B)+NUL 填充（长度/节表/偏移不变，不违反「ELF 不做变长重写」）；白名单而非全量，因为 node/dash/make/tar.real 等 20+ 个 ELF 含同一字面量但不在因果链上。判据：`python3 scripts/relocate-snapshot.py --self-test`（8 项，含「非白名单 ELF 不得被改写」反证，关掉白名单即 4 项判红）+ 设备端新增 P4b/P4c。真品实测：3,562,128 B 经管道后长度不变、与原文只差 37 字节（区间 362813–362849）、旧串 0 次、helper 与 `store` 往返均通。残留：`pkg upgrade git` 会复发（P4b 静态断言即为此）、`/system/bin/sh` 是 mksh 而非快照内 dash（要指回 dash 只能重建包）。 | wen0531 |
| 2026-09-22 | **修复：文件写工具在 Android 上建不了新文件（坑 171 / apk issue #246）** | `dsh-fs-local` 的 `createIfAbsent` 发布站点是全包唯一的 `link(2)` 调用且失败即抛、无任何回退；Android 应用域恒拒 hardlink（EACCES）⇒ 真机上 `write` 工具**建不了任何新文件**，而覆盖已存在文件正常（走 `rename`）——症状为「只能改不能建」。定位到退役记录的空洞：§「已退役资产」称 link(2) 回退族「改由构建期补丁承担」，但 §7.1 表里没有 `dsh-fs-local` 行、快照内也 grep 不到既有回退标记（0 次）；EngineManager 注释「fs-local rename fallback is upstream-native」对 0.1.5-rc.1 不成立（上游把 rename 用在替换路径，新增的独占创建路径又裸用 link）。修法：`EACCES/EPERM/ENOTSUP` 时改用 **O_EXCL 占位 + rename** 等价实现 no-replace（保住独占语义：输家仍得 `EEXIST` 与同一拒绝文案），`rename` 失败回收占位防 0 字节残留；**双路同源**——构建期补丁 `fs-local-link-F8`（registry + IMPLS）+ 运行时资产 `fs-local-index.js`（41,407 B，`:767` 注册）。行为回归 `scripts/patches/tests/fs-local-link-f8.test.mjs` 19 项（含 `--asset` 直测资产本体），并登记进 `check-runtime-assets.mjs` 的 `BEHAVIOR_TESTS`。**教训**：退役理由「上游已原生覆盖」须逐站点核对——上游会往同一文件里继续加新的 link 站点。 | wen0531 |
| 2026-09-22 | **修复：phone-control 预设在任何干净安装上恒「加载失败」（坑 170）** | `seedPhoneControlPreset` 对 `SKILL.md` 直接 `readText()`：文件缺席时抛 `FileNotFoundException`，被外层 `catch(Throwable)` 吞掉 ⇒ 函数当场返回，`agent.cordis.yml` / `preset.yml` / `customSkillDirs` 注入全都没跑；而幂等早退判据正是 `preset.yml` ⇒ 每次启动从同一行重炸、**永不自愈**。真机取证：logcat `seeding failed` + `open failed: ENOENT`（`EngineManager.kt:475`）、目录四层 mtime 纳秒相同（一次 `mkdirs()`）、同 uid 手写同路径成功（排除权限/属主/路径）。回归出处 = #141「SKILL 每次启动刷新」（其验证是「设备侧已热修」，前提是文件已在场 ⇒ 干净安装路径从未被测），#190 追加内容时同样只验了存量升级路径。修法：`if (skillFile.isFile) skillFile.readText() else null`，缺席视同需要刷新（对已有 SKILL.md 的设备语义逐字等价）。判据：`CallSiteContractTest.phoneControlSeedingToleratesAnAbsentSkillFile`（真源表达式在场 + 裸读形态消失，撤掉守卫即红）。 | wen0531 |
| 2026-09-19 | **0.14.1 预览轮 / 三个 P0 设备缺陷修复（未提交/未验收，分支 feat/0141-preview）** | **事实收口**：`caps` 只随控制 op 回执抵达，`android_capabilities` 自己不入队 ⇒ 冷启动必然报「Shizuku 未就绪」而壳侧已授权（模型据此放弃可用能力）。修法：三态（就绪/实测未就绪/**未探测**）+ caps 缺席时引擎补探一次（`AndroidPrivilegeService.shizukuChannelProbed()`，TTL 10s，用不受档位门约束的 `vdInfo`；探测失败保持 undefined，**绝不降级成 false**）+ 三分文案（「未知」必须给出「直接试一次」的可执行动作）；`android_privilege_status` 增独立特权通道行，`v.message` 不再挂「ADB 提示」标签；特权状态路由与工具同步补探。**工具面**：壳侧 `REAL_SCREEN_OPS` 从 11 条**收敛到 8 条**（`state`/`webSnapshot`/`webAction` 是 `A11Y_OPS` 的陈旧拷贝、都不读设备屏，却因不带 `screenId` 被范围门按真实屏拦死 ⇒ 缺省 virtual-only 下 android_web_dump、WebView ref 路径、点击生效校验全不可用）；`android_act_input` 目标为虚拟屏时改走 `vdInput`（text 直传不受 ASCII 限制）；`android_ui_tree` 删除它无法兑现的 `screenId` 参数；**补实现 `android_vdisplay_input`**（tap/swipe/keyevent/text，虚拟屏按键与文本此前在工具面无路可达；此前被三处文案与 Skill 文档承诺却从未 defineTool）；虚拟屏组工具 3→4，注册集基线 16772→18490 B（初始可见集不变 2951 B，新工具受渐进披露掩蔽）。**落点可证**：`VdisplayController.launchApp` 增加落点回读（`am` 退出码 0 ≠ 落在目标屏，设备实测已有 task 时只打印 warning 仍回 0）——按 `Display #<n>` 分组解析包的显式 ActivityRecord，三态回报（`vd-launched` / **`vd-launch-denied`** 带 landedDisplayIds / `vd-launched-unverified` 明写不构成证明）；为避开 UserService 的 16 KiB stdout 截断（会恰好吃掉虚拟屏那一段）改用固定字面量先过滤（42 KB → 1.97 KB）；引擎侧 schema 增 `screenId/displayId/landedDisplayIds/code`。**新门禁（3 条，均已接线两条链）**：`check-op-registry-parity.mjs`（跨语言 op 清单逐条相同）、`check-tool-name-promises.mjs`（指引承诺的工具名必须有声明位）、`verify-screen-scope-matrix.mjs`（设备套件：跨面一致性 + 落点回读 + 双屏像素对照 + real-only 反证，判据全落在设备事实上，证据不足判 INCONCLUSIVE 而非通过）。**判定性实测**：① `uiautomator dump --display <n>` **被收下但不生效**（无参/--display 2/--display 0 三者逐字节相同）⇒ 不得放开该家族，只修上层工具面；② `am start --display 2` 在本机对第三方应用有效，但已在真实屏有 task 时只打 warning 且退出码 0。**文档**：坑 161-165、known-gaps 三条更正、协调仓缺陷定性文档同步。 | kelai141 |
| 2026-09-19 | **0.14.1 预览轮 / 三层验收约定（未提交/未验收，分支 feat/0141-preview）** | **测试规范升级（用户拍板）**：`AGENTS.md` §2.1 由「双轨」升级为**三层验收**——代码层 + CDP 层 + ADB 用户实际体验层，**缺一层即未验收**；**任一层红即这个系统有问题**，必落在四类之一（不符合人体交互逻辑 / 在用户视角里就是坏的 / 代码有问题 / 内部没打通），禁止当噪声、禁止只修那一层，并给三层的对照读法。新增**任务形态强制**：验收只给「目标 + 约束 + 成功标准」，**步骤必须由模型自己编排**，禁止把步骤一条条喂给它、禁止单步或两三步的简单操作；过程按工具调用序列 + 设备截图 + 最终汇报留证，**工具自报成功不算达成**。铁律 #2 与 §3 路由同步改口。详档 `docs/AGENTS/emulator-test-protocol.md` 同步：新增 §0 三层验收（对照表 + 四类读法 + 禁止只修红层）、§10 真实任务验收（形态/留证/反证），证据判据改三层、反模式清单补第 8-10 条。**动因**：0.14.1 三个 P0 缺陷（工具面报 Shizuku 未就绪、virtual-only 下工具大面积不可用、跨屏拉起报假成功）全部满足「代码层与 CDP 层都绿、只有真机用户层暴露」，定性见协调仓 `docs/0.14.1-preview-DEVICE-DEFECT-TRIAGE-AND-TEST-REFLECTION.md`。 | kelai141 |
| 2026-09-19 | **0.14.1 预览轮 / 执行地图与模拟器测试规范（未提交/未验收，分支 feat/0141-preview）** | **执行地图**：新增 `docs/AGENTS/EXECUTION-MAP.md`（19 查点：主表 / 耦合矩阵 146 边 / 症状索引 101 条 / 疑点分级 26 条高危 / 20 张流程图 / 覆盖账本 184 项），门禁 `scripts/check-code-map.mjs`（覆盖 + 锚点 + 编号一致性，8 例判别力自检，`--no-parent` 可预演 CI 独立检出）并接入 apk CI。**模拟器测试规范**：`AGENTS.md` §2.1 强制双轨验收（CDP 断言 + adb 截图/点击）+ 详档 `docs/AGENTS/emulator-test-protocol.md`（原语/证据/判据/假失败清单/反模式）。**排查实修**：`jsString` 的 U+2028 转义是恒等替换（生产没修、单测因另一份 org.json 假绿）→ 改纯函数形态断言，9 例全绿；坑 159/160 登记。**漂移**：ARCHITECTURE.md 头部数字口径与已删除的 AdbState.kt 行、pr-gate 的 417→456、其余 50+ 条登记进地图 §8。 | kelai141 |
| 2026-09-19 | **0.14.1 预览轮 / P0 真机 API 越级崩溃修复（未提交/未验收，分支 feat/0141-preview）** | **真机反馈（华为 NOH-AN00 / Android 31 / 出厂 0.14.0 vc39）**：`engine-died-during-boot` exit=1，logcat 12:55/12:56/12:57 三时点反复 `NoSuchMethodError: No interface method toList()Ljava/util/List; in class Ljava/util/stream/Stream;` → `SnapshotFs.deletePath(SnapshotFs.kt:50)` → `SnapshotTransaction.finish` → `EngineManager.applyRecovery`。**真因**：`Files.list(dir).use { it.toList() }` 的 `toList()` 是 **Java `Stream.toList()`（API 34）**而非 Kotlin stdlib 扩展；minSdk 26 → API<34 设备无此接口方法。**后果链**：`NoSuchMethodError` 是 **Error 不是 Exception**，`catch (e: Exception)` 抓不住 → 打穿恢复链 → **快照刷新/恢复在 Android<34 永久卡死（marker retained）**。**修法**：改用 `Files.newDirectoryStream(dir).use { it.toList() }`（`DirectoryStream` 是 `Iterable`+`Closeable`，Kotlin `toList()` 是 stdlib 扩展，无 API 级别依赖；`use` 保证关闭；语义等价：只列直接子项、不跟随符号链接、项读取失败记 onFailure 并跳过）。**全仓 API 审计（115 个 .kt）**：命中 1 处（即本坑），已修；`SnapshotUserData.kt:106` 的 `Stream.forEach`（since=24）与 `SnapshotTransaction.kt:240` 的 `File.listFiles()` 均判安全；全仓 23 处 `.toList()` 其余 22 处接收者为 Kotlin 集合/Sequence（安全）。**新增防线** `ApiLevelGuardTest`：禁用清单（`Stream.toList`/`ofNullable`/`dropWhile`/`takeWhile`、`Collectors.toUnmodifiable*`、`Files.readString/writeString/mismatch`、`String.strip*/stripIndent/formatted`、`List|Map|Set.of`、`requireNonNullElse`）+ **词法扫描器**（先剥字符串再剥注释，兼容跨行链式），带 13 项反证与零假红自检；**并修掉扫描器自身的自伤缺陷**（朴素「本行含块注释起始符」实现被 MIME 字面量误入块注释态、静默漏扫 3273 行/18%）。**注**：`isBlank()` 经 javap 对 `app/build/tmp/kotlin-classes` 实证解析为 `kotlin/text/StringsKt.isBlank`（stdlib，安全），**不**列入禁用以免大面积假红。**文档**：坑 153、modules.md、ARCHITECTURE.md。 | overlayui |
| 2026-09-19 | **0.14.1 预览轮 / T5 块H+块I（未提交/未验收，分支 feat/0141-preview）** | **块I 悬浮球**：取消贴边强制吸附（删 `springSnapToEdge`/`cancelSpring`/`springAnim` 与 `androidx.dynamicanimation` 三个 import；壳侧该依赖从此零使用方，声明按详档判定保留并登记用途变化），保留最小边距并抽成唯一具名常量 `BALL_EDGE_MARGIN_DP=8`/`edgeMarginPx`（同时推导 `haloSizeDp`，整数恒等式被测试锁死），就地更正三处过期注释。新增状态描边 ring：`OverlayHalo.newHaloDrawable` 返回 **glow+ring 两层 `LayerDrawable`**（ring 硬边描边 2dp、内缩 4.5dp，落在球半宽与窗口半宽之间的可用环带内），`setHaloColors` 单函数同时写两层、颜色**同源**取自 `Halo.color`，`setHalo` 仍是唯一入口且脉冲逻辑一字未改（同 View → 天然同相位）；`as? GradientDrawable ?: return@post` 式静默失败改为显式取层 + `LogCollector` 诊断；`Halo` 枚举改**纯 Kotlin ARGB 字面量**（原 `Color.argb` 在 `isReturnDefaultValues=true` 下恒 0 → 颜色类单测假绿）。**块H 悬浮窗**：完成态常驻文案「已完成，长按查看汇报」——权威信号 `api-session/status running=false` 置位、语义标签取 `.live.ndjson turn_end.kind`（失败/被阻塞/被中断不得显示「已完成」），完成位放**服务级** `CompletionNotice`（面板收起态存活），`showPanel()` 首次打开消费一次、收起即回常态、新一轮与 tool_call 清除；报告栏长按打开（新增 `OverlayReport.kt`：独立顶层窗口 + ScrollView，内容取 T6 窄接口 `NotifyStore.latestReportLine()` 并经既有 `NotifyStore.parseEntry` 解析，复用 `reportLine/reportBigText` 文案口径；空摘要/无数据给占位行仍可打开；hidePanel/onDestroy 两处收口）；三击跳转（单一 OnTouchListener 状态机，长按优先、超 `scaledTouchSlop` 判拖动、`NEW_TASK\|SINGLE_TOP` 回 MainActivity 不重建 WebView）；长按期间置 `panelOccupied` 守卫自动收起。**测试**：新增 `OverlayHaloInvariantTest`（四态互不相同/字面量逐字节等价/多 density 恒等式与 ring 环带）+ `OverlayCompletionNoticeTest`（A1 无 pending 普通完成必显示、复位反证、语义不伪装、A2 空摘要与无数据不崩、A3 二次点击与长按互斥），`CallSiteContractTest` 追加 15 条断红线。**门禁**：`check-kotlin-comments.mjs` PASSED。**文档**：坑 147-150、modules.md、ARCHITECTURE.md、BRIDGE-API.md、DEPENDENCIES.md。 | overlayui |
| 2026-09-19 | **0.14.1 预览轮 / T6 块J+块L+块K壳侧 + 探针接线（未提交/未验收，分支 feat/0141-preview）** | **块J 通知实时化**：FIX-3 用户拍板取 A —— `DEFAULT_SUPPRESS_FOREGROUND = false`（前台也真发系统通知）；存量迁移**不动 prefs**（旧抑制系默认值造出、`setSuppressForeground` 全仓零调用，改默认值即修好存量；显式值原样保留并备份留痕、schema 代次只跑一次）。FIX-1 抑制=延后：新增 `NotifySuppressQueue.kt`（TTL 5min/容量 8/单次补投 3/同会话覆盖式去重/自续 tick），补投复用 `deliverDeferred`。FIX-2 listener 真实实现（`ShellListener` + `installShellListener`，复用 `flashStatus`、只依赖既有公开面）。FIX-4 设置读写能力（`settingsSnapshot`/`applySetting`，写后读回、未知 key 拒绝）。**块L**：`writeBootDiag` 落 `files/boot-diag.log`；页面侧 fiber 状态不可得时显式 `pageSideRuntime=unavailable`（**绝不留空数组**）。**块K**：NSC base-config 撑开明文 + 回环保留；`check-manifest-hardening.mjs` NSC 段同批改新形态（显式声明/回环逐字比对/拒无 domain 明文口子，**实测回退即判红**）+ JVM `BrowserHostCleartextConsistencyTest` 守跨层同向；**#183 影响书面记录**。**探针接线**：真因 = 产出面缺席（`[perf] TOTAL` 仅测量 preload 产物，且不在快照/overlay/注入链、argv/env 均不含）+ 解析链挂在**默认关闭**的调试采集器上；修法与 C1「首个 HTTP 响应」口径见坑 144。**文档**：坑 144-146、modules.md、ANDROID-API-USAGE.md。**交付**：T5 窄接口 `NotifyStore.latestReportLine(): String?`。 | shellcore |
| 2026-09-18 | **0.14.0 正式版（vc39，覆盖 v0.14.0-preview vc38）** | **本版主题：虚拟屏与 AI 浏览器达到可用线。** **虚拟屏**：跨屏拉起四条候选路径实测只有「壳侧 Shizuku UserService + `am start --display <id> -n <component>`」成立（monkey 无屏幕维度 / shell `am start` 不稳 / 进程内 `setLaunchDisplayId` 被 SafeActivityOptions 拒），据此把唯一可行路径做通；补 `screenId` 可发现性（此前该参数无 description，设备实录证明模型直接忽略 → App 落到真实屏）；截图两条通道都认屏幕（ADB 回落此前完全忽略 screenId，抓的是真实屏——静默错误答案）；等比 letterbox 按内容宽高比居中；可见性判据补「侧栏已收起」；序号复用（旧实现单调递增从不回收）；会话隔离 + 销毁不设锁 + 10 分钟空闲回收；手动关机按钮。**AI 浏览器**：隔离宿主 WebView 可用；分辨率=CSS 视口（document-start 注入 + letterbox，innerWidth 精确）；PC/手机身份；多页签；browser_snapshot 渲染每行 ref；侧栏收起时视口不再塌成 0x0（GONE → INVISIBLE + layoutDetached，实测 0x0→450x800）；setInitialScale 在 API 36 stub 已移除 → 改注入实现。**附件入口分流（#215）**：回形针单击即出菜单（Tooltip 气泡插在按钮与 input 之间致兄弟相邻判据失效，改为结构判定）；显式图片类型分流到系统相册（此前壳侧为修「空 accept 受限视图」把相册分支整个删掉，两个入口打开同一界面）。**Shizuku 时序**：ensureBound 只 await 一片 4s 而 bind 实测约 5s、且 bindLatch 一次性不清空 → 「第一次必失败第二次必成功」是确定结果；改为总预算内循环等待 + 三路径清 latch + connecting/not-bound 拆成两个 code。**快照升级健壮性**：deletePath 逐项容错 + 残留 stage 改名挪开（此前一个删不掉的损坏 stage 会让之后每次启动都失败）。**坑位 139-143 登记**（Termux 报错风暴真因 / 回形针兄弟判据 / Shizuku latch 时序 / 截图忽略 screenId / 图片 accept 被壳侧统一入口吞掉）。**验收**：门禁全绿、Kotlin 269 项 0 失败、插件 82+62+12、UI 180、双 ABI exit 0。 | kelai141 |
